for (let ; ;)
    do {
        index;
    } while (condition);
0;
index < array.length;
index++;
{
    const element = array[index];
    export default 8086;
    export const open = ['index.html'];
    export const host = '0.0.0.0';
    export const https = true;
}
